// UI type definitions for rendering components
// Common types used across UI rendering system
// Defines styling, layout, and interaction types
export interface Rect {
  // TODO: Define UI type interfaces and styling types
}