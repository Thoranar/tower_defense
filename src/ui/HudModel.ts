// Data model for HUD rendering
// Structured data passed to UI renderer for HUD display
// Contains health, XP, level, time, and boss information
export interface HudModel {
  // TODO: Define HUD data model structure
}