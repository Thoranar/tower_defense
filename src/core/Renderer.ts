// Abstract rendering interface
// Defines the contract for rendering entities and UI elements
// Allows swapping rendering implementations (Canvas2D, WebGL, etc.)
export interface IRenderer {
  // TODO: Define abstract rendering methods
}